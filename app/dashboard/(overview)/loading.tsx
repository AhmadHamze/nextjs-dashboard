import DashboardSkeleton from "@/app/ui/skeletons";

// * This file exists in (overview) folder (note the parenthesis), so that it doesn't apply to the pages in customers/ and invoices/
export default function Loading() {
  return <DashboardSkeleton />;
}
